build:
  docker:
    phpfpm:
      dockerfile: Dockerfile
      target: php-prod
    nginx:
      dockerfile: Dockerfile
      target: nginx-prod
    frontend:
      dockerfile: Dockerfile
      target: frontend